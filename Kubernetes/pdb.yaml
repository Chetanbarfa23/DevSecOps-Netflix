apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: netflix-pdb
  namespace: default
  labels:
    app: netflix
    project: devsecops-netflix
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: netflix
